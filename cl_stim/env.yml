name: cl_stim
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - pyserial
    # MATLAB engine cannot be installed via pip/conda directly from the web.
    # It must be installed manually from your local MATLAB folder.